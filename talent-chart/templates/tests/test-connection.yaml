apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "talent-chart.fullname" . }}-test-connection"
  labels:
    {{- include "talent-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "talent-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
